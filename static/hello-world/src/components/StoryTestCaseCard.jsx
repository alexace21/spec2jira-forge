// DEPRECATED / REMOVED 2026-06-27 — the per-story <details> accordion was replaced by the two-tier Test
// Cases redesign: a compact triage `StoryRow` (overview) + a drill-in per-story type-phase `StoryWizard`,
// both in ./StoryWizard.jsx. The per-story DRAFT/Save/Revert/coverage/export model is unchanged (it lives
// in TestCasesScreen). This file is intentionally empty; delete it in a tidy-up commit.
export {};
